function [handle, cost] = zeta8(u)
% Returns the handle and cost for next 
% state for input u at state zeta8
    switch u
        case 0
            cost = 0;
            handle = str2func('zeta8');
        case 1
            cost = 0;
            handle = str2func('zeta8');
        case 2
            cost = 0;
            handle = str2func('zeta8');
    end
end